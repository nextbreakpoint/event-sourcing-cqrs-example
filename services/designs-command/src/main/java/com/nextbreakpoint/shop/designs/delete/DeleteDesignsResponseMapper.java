package com.nextbreakpoint.shop.designs.delete;

import com.nextbreakpoint.shop.common.ResponseMapper;
import com.nextbreakpoint.shop.common.Result;

public class DeleteDesignsResponseMapper implements ResponseMapper<DeleteDesignsResponse> {
    @Override
    public Result apply(DeleteDesignsResponse response) {
        return new Result();
    }
}
